<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <div class="header-upper">
            <div class="container">
                <div class="header-content">
                    <div class="header-logo">
                        <img src="/images/logo.png" alt="Uygur Haber Ajansı">
                    </div>
                    <div class="header-branding">
                        <h1><a href="/">Uygur Haber Ajansı</a></h1>
                        <p class="tagline">AI Destekli Haber Platformu</p>
                    </div>
                    <div class="header-widgets">
                        <!-- Widget area for future use -->
                    </div>
                </div>
            </div>
        </div>
        
        <div class="header-lower">
            <div class="container">
                <nav class="nav-container">
                    <ul class="nav-tabs">
                        <% navigation.tabs.forEach(tab => { %>
                            <li>
                                <a href="<%= tab.url %>" class="<%= tab.active ? 'active' : '' %>">
                                    <%= tab.name %>
                                </a>
                            </li>
                        <% }); %>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <main>
        <div class="container">
            <article class="news-detail">
                <div class="breadcrumb">
                    <a href="/">← Anasayfaya Dön</a>
                </div>
                
                <h1 class="article-title"><%= news.title %></h1>
                
                <div class="article-meta">
                    <span class="article-date">
                        <%= new Date(news.published).toLocaleDateString('tr-TR', {
                            year: 'numeric',
                            month: 'long',
                            day: 'numeric',
                            hour: '2-digit',
                            minute: '2-digit'
                        }) %>
                    </span>
                    <% if (news.link && news.link !== '#') { %>
                        <a href="<%= news.link %>" class="source-link" target="_blank" rel="noopener">
                            Kaynak Haberi Görüntüle
                        </a>
                    <% } %>
                </div>

                <% if (news.image) { %>
                    <div class="article-image">
                        <img src="<%= news.image %>" alt="<%= news.title %>">
                    </div>
                <% } %>

                <% if (news.summary) { %>
                    <div class="article-summary">
                        <strong>Özet:</strong>
                        <p><%= news.summary %></p>
                    </div>
                <% } %>

                <div class="article-content">
                    <%= news.content %>
                </div>

                <% if (news.tags) { %>
                    <div class="article-tags">
                        <strong>Etiketler:</strong>
                        <span><%= news.tags %></span>
                    </div>
                <% } %>

                <div class="article-footer">
                    <a href="/" class="btn-back">← Tüm Haberlere Dön</a>
                </div>
            </article>
        </div>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2024 Uygur Haber Ajansı. Tüm hakları saklıdır.</p>
        </div>
    </footer>
</body>
</html>
